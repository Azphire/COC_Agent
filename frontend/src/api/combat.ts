export type CombatWeapon = { id: string; name: string; kind: 'melee' | 'firearm'; skill: string; damage: string; ammo: number; reserve: number; quantity: number; jammed: boolean }
export type Combatant = { id: string; label: string; member_id: string | null; team: string; incapacitated: boolean; hp?: number; hp_max?: number; armor?: number; source?: string; weapons?: CombatWeapon[]; injury?: Record<string, boolean | string | number | null> }
export type CombatPending = { id: string; stage: string; actor_id: string; target_id: string; participant_id?: string; operation: string; weapon_kind?: string; summary: string; rolls: Record<string, { raw?: { total: number; level: string }; result: { total?: number; level: string; passed: boolean }; luck_spent?: number }>; luck_options: { spend: number; result: { total: number; level: string } }[] }
export type CombatView = { active: boolean; round: number; order: string[]; current_actor_id: string | null; turn_key: number; participants: Record<string, Combatant>; pending: CombatPending | null }
