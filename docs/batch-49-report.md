# 第49批：自然调查与开团恢复

基线：`dc972f10674ff8b6878d1bde1ae9b45e06689b14`（`random ai player`）。本批不改第48批计划、报告、原失败记录及已采用旧人物，不自动commit/push。

本批代码与验证记录已交付，**整批验收未通过**。读便签、人物保卡文字修复及向导恢复通过；自然委派的路由与本次实际揭示账本通过，但KP正式正文仍未答全，完整请求结算和该题成功流式／重连未通过。以下将实现、离线验证、真实浏览器假模型流程和真实模型结果分开记录。

## 自然调查修复

- 原文恢复从最终选择的公开事实／记忆投影形成同一证据视图，必须保留原事件引用。去重不再让空的另一投影抹掉已召回的便签原文；模型原稿、私密记录、无引用及待办不能补造来源。
- 当前未点名问题归KP，混合观察与提问分别形成正文需求。便签原文进入对应需求的来源集合；附属细节、覆盖字段和旧开场不能代替正文。纯原文复述限定当前所问来源，不整段输出全部历史。
- 旧重叠姓名按完整称呼优先解析到唯一成员ID，去掉界面席位后缀后统一登记与调度。真实歧义要求澄清；建议、引用和否定不变成实际委派。徒手检查的手是方法、便签是对象，真实工具仍校验本人物品实例。
- 当前实际揭示进入结果账本，保留执行者、动作目标、揭示目标与原事件。旁白完整性单独记录；旧揭示、其他回合、错误请求key／执行者／目标／操作不能完成本次请求。旧编辑自主child没有请求key，不替猎人84:7结算；只读观察仍要求当前有效正式答复。

来源与正文最终定向组 **180通过，7.14秒**，见[证据验证](../data/prepared/batch-49/evidence-validation.md)和[日志](../data/prepared/batch-49/evidence-final-09.txt)。包括冻结首败、受影响原文、正文、来源、条件、完整人格投影及完整结果正文／纯观察体裁边界；重复运行不累计。Ruff通过。一次旧集成夹具模型目录准备失败及测试过程中未通过的检查单列在证据验证中，未算通过。

委派与结果组先112项通过，引用边界与旧称呼组63项通过；真实首败定位后的当前结果／任务组122项通过，最后新增28项冻结边界组通过；纯称呼残片受影响组94项通过，旧称呼回归29项通过。分别见[委派结果验证](../data/prepared/batch-49/delegation-validation.md)，组合有重叠，不相加为唯一总数。没有用旧揭示、错误key、执行者、目标或操作结清新请求。

完整正文变更后，独立当前结果／委派／结算组101通过、30.37秒，另增加真实持久化结算边界2通过、3.73秒。已验证正式正文报告真实结果并明确结构未知，可以完成检查与报告义务；相同正文未验证仍不能完成。完成任务不表示已经证实无夹层／折叠，未知内容保留在回执中。

## 人物文字与开团恢复

每次显式“只修复人物文字”仅调用一次模型，自动继续仍有4次上限；累计调用、失败稿、修复次数与耗时保留。修复保留数值卡、原骰、seed、HO、已定公开姓名及其他成员。HO私密文字继续按原权限投影。文字从实际技能强弱与批准资料生成；生成、编辑、采用复用一致性检查，不加数值或逐回合审核模型。

开团按核准模组人数、本人及预留真人席位生成合法默认AI数，并在模型调用前检查。未采用队伍可明确调整人数，保留留下成员与HO。本人／队友批次创建即在事务中与带版本的服务端草稿关联；恢复重新读取服务端人数、年代、HO与进度。继续旧游戏支持原地主机解锁，返回原房间玩家投影。

人物假模型定向66项通过；包括4次自动耗尽后显式修复、幂等与累计审计、HO隐藏、姓名编辑／本人卡／全队采用边界，以及冻结“失去视力／生存41深厚造诣”反例。详见[人物验证](../data/prepared/batch-49/persona-validation.md)。未跑669卡全矩阵。

恢复后端17项通过，33.57秒。真实Chrome与HTTP／WS验证前置超员0生成调用、生成中刷新、未采用本人卡清缓存和第二浏览器恢复、队伍减少人数保留完整成员JSON、真人预留与现代年代恢复、冷旧房间原地解锁后玩家投影。复用已有成员，后续恢复／减少人数／冷启动均0新增调用；总共4次Fake文字调用，不作为真实模型性能证据。首轮超员提示漏显示及同页清凭据后解锁状态不刷新均保留失败并修复，仅复验相应边界。详情及独立报告链接见[向导恢复验证](../data/prepared/batch-49/launch-validation.md)。

受控真实文字修复使用当前 `ollama/qwen3:8b`、16384窗口：

| 运行 | 结果 | 本次调用／usage | 耗时 |
| --- | --- | --- | --- |
| real-persona-01 | 输出仍加入无来源的失去视力状态，被新一致性检查拒绝；保卡／姓名／seed／其他成员／旧审计均通过 | 1；2668 input／139 output | 30.093秒 |
| real-persona-02 | 从01失败后文档续同一人物，仅修文字成功；累计4→5，保留全部01失败 | 1；2710 input／147 output | 8.218秒 |

两次合计5378 input／286 output，服务耗时38.311秒。成功稿以机械维修79、斗殴71和人类学1的实际强弱描述普通工作经历；化石仅是兴趣，未增加身体限制、装备或资质。数值卡（含原骰／HO）、已定姓名、seed及其他成员前后完全一致。此真实验证没有HO，HO权限另由假模型定向证明。

## 真实模型验收

在新的第49批隔离目录复用三张旧合法人物卡、原人格、已批准《常暗之厢》和知识绑定，用正常HTTP新建验收房间。旧重叠姓名原样保留；开场0生成调用，便签背面初始为hidden。没有填窗、压缩、备团或整模组重跑。`real-01`仅为可选配置文件缺失造成的准备失败，0模型调用；后续允许从现有.env配置启动。

`real-02`原始首题已校正问题归属，两稿正文均实际回答观察和便签原文，但现有覆盖检查把“贴着一张便签”的不定单数判为无来源数量，阻断原有正文／引用映射规范化，正式事件仍为不完整回退。该次失败不计通过。保留[原题审计](../data/prepared/batch-49/real-02/read-case.json)，针对该真实输出补8项冻结及反例，只允许已选原文对象的不定单数；明确总数、两张、其他对象仍拒绝。规范化不改正文、不换来源，仅从实际已答内容重建映射，连同现有覆盖与来源共64项通过。随后只复验同一原题。

`real-03/read` 同一句冻结原题通过：初稿缺少便签原文被拒绝，一次修复后的正式e46同时写出环顾结果与“只管前进吧，已经没有退路了。”，`answer_complete=true`。共3次模型调用、68.909秒。成功attempt2的stream `f7cb8c29-de76-4549-85de-98dd91ed5486` 在两窗有本次delta、第二窗响应中断线重连和stream.end；最终每窗恰好一个正式e46且与history逐字一致。初稿首DOM37.596秒，成功稿首DOM49.987秒，正式DOM63.988秒；成功稿实际仅1次delta，不能混入初稿2帧声称更多增量。见[本次浏览器报告](../data/prepared/batch-49/real-03/browser-observer/report.md)。

`real-03/delegate` 原始委派明确进入猎人child，短姓名编辑没有被唤起。初始hidden的便签背面在本child产生新e96，e97为search回执，e102结果账本保留实际执行者、目标及源动作。两份旁白未通过，正式e104仍是回退，原请求53:7保留observe未完成；因此只计路由／真实揭示账本通过，KP正文与完整结算失败。共6次调用、140.954秒。失败正文中的“没有夹层或折叠”没有批准来源，不能作为成功证据。

该失败揭示三个具体边界：词汇匹配把问题“有没有夹层”与旧原文“没有退路”误关联；实际揭示没有独立必答需求；同请求后续“纸张”的操作目标未绑定到前述唯一纸质对象。修复将严格匹配当前cycle／执行者／源动作／目标及原事件的实际效果加入正文需求，未知检查细节须明确未知，保留动作与结果分别校验。冻结失败仍拒绝，新增反例覆盖同句未知掩盖无来源肯否、重复映射、旧源、只说动作和遗漏真实结果。后续只在新的初始hidden准备状态复验原委派，不重跑已通过读便签，不以旧e96代替新揭示。

`real-04/delegate` 仍失败：新e70／e71／e76保留真实揭示、search及账本，25:7仍有observe未完成。父e46、子e78都是回退；两次失败稿的delta／重连只记失败证据，不借用real-03/read成功流。共8次调用、171.763秒。父计划剥掉请求正文后残留“林修远·猎人，”，被误当作自行动作，额外产生2次KP失败旁白；子回合虽已有正确的未知／实际结果需求，模型仍借e70断言纸张无夹层，修复稿又重复开场，均被拒绝。

针对real-04，生成语法在出现未知需求时逐项绑定需求ID、自己的来源和状态：未知项只能无来源／unknown／空来源引文，仍必须在正式正文实际说明未知。其余需求不能借用该项或别项来源，保留逐项及全文校验。此约束不从附属字段补正文，也不把失败稿改成成功。称呼修复只剔除同一解析器确认的纯称呼／标点残片，保留并存自然动作；已有委派专属路径因此跳过父KP。

`real-05/delegate` 父回合已无多余KP；猎人将行动简写为“我试着用手检查便签边缘。”，新e68／e69／e74继续保留真实效果。但子回合需求只剩笼统“便签边缘”，丢掉原请求的夹层／折叠问题，未知项生成约束未启用；两份旁白仍失败，e76正式回退，25:7的observe仍未完成。6次调用、140.781秒，0成功正文delta／重连。失败不计通过。

针对real-05，把当前child已冻结的原请求带进正式答复：只取本child request_keys，逐项核验原公开事件及精确span、执行者、操作、目标和目标来源证明；不同对象、旧任务或未授权操作不继承。原话只是需求，不能作为结果证据。相同对象的具体检查目的保留，实际揭示另有原事件来源；行动简写不能删去夹层／折叠问题。冻结接线与运行时组87通过，新增原请求绑定组30通过，详见委派验证。

`real-06/delegate` 原请求25:7已完整进入child，未知／当前结果两项需求与生成字段约束正确；但模型首稿仍无来源地否定夹层／折叠，仅在覆盖字段填实际原文；修复稿又重复开场。e76正式回退，observe仍未完成；实际新e68及账本继续保留。6次调用、146.321秒，本次增量与重连最终interrupt，明确不算成功流。

进一步核对实际发送消息发现：最终answer_sources只有本次e68，但allowed_facts、current_scene、historical_memory和observation_subject又带入未选的旧开场／正面原文；answer_basis还沿用执行前plan的unrecorded而变成improvise。最终修复在KP当前结果契约中统一生成投影、claim枚举和覆盖来源，仅发选定内容，实际回执覆盖旧即兴判断；同模式去掉外观即兴许可。原run.context仍完整保存，权限、来源、防复读和结果账本校验不变。冻结06投影证明旧开场彻底退出提示；混合需求选中的场景来源仍保留。

`real-07/delegate` 不再重复开场，但两稿仍只在覆盖字段填写实际原文，正文无来源断言“没有夹层或折叠”。新e68／e69／e74保留实际效果，e76正式回退、25:7仍有observe未完成。6次调用、139.878秒；两稿0正文delta，没有成功流式或重连。失败原稿与浏览器证据保留。

检查输出契约发现含实际结果的普通观察仍强制使用 `observed_detail`。改为含question或result的需求使用现有完整 `public_narration`，纯观察保留原字段；完整正文保留实际结果与具体未知说明，流式字段选择及全文来源／覆盖校验继续复用。这是针对07输出体裁的修复推断，离线正确稿不替代真实通过；随后仅准备real-08同题复验。

`real-08/delegate` 两稿完整正文已实际写出e68背面的原文，但仍无来源断言纸张无夹层／折叠，正式e76继续回退，原请求25:7的observe保持未完成。6次调用、149.552秒，29967 input／2151 output；两稿各有本次增量及重连快照，最终都是interrupt，没有成功end。实际发送消息没有计划的虚构success_effect或否定来源，唯一来源仍是e68；保留失败证据，不因结果句有效而计整段通过。

08的system仍含普通观察可即兴补外观的通用指令，末尾再声明当前结果例外。随后仅在已有实际结果的KP正文模式替换为简短专用指令，明确操作成功不代表全部被问性质已知；无来源需求给模型提供具体“尚未确认”表达提示，修复反馈也逐项指出。提示不进入answer_sources、有效覆盖或正式正文，不修改模型稿；自定义指令、NPC和纯观察模式不替换。冻结08两稿仍拒绝，受影响覆盖／wire／提示边界 **100通过，1.19秒**，见[evidence-final-10](../data/prepared/batch-49/evidence-final-10.txt)。后续仅real-09复验同题。

`real-09/delegate` 两稿不再否定夹层，但未知句只在answer_coverage，正式正文仍只写动作与e68实际结果，因此e76回退、25:7仍有observe未完成。6次调用，27629 input／1737 output，128.640秒。两稿分别各1delta及同stream重连快照，最后均interrupt，0成功end；浏览器真实证据不计成功流。

针对09，生成schema只对“KP完整结果正文且有无来源需求”要求正文实际含“尚未确认”；它不补写正文、不从覆盖字段拼接答案，也不代替逐项对象、来源、数值和前后断言检查。有源结果、NPC和纯观察不要求该固定措辞。局限是该模式需用此明确未知表达，不能只用其他同义词。冻结09两稿、错对象以及先否定后未知都继续拒绝；已答正确正文仍通过。最后定向 **104通过，1.23秒**，见[evidence-final-11](../data/prepared/batch-49/evidence-final-11.txt)，再收窄keeper范围的33项通过0.56秒；与前序组合重叠不相加。随后仅real-10同题复验。

`real-10/delegate` 仍失败：两次实际发送的output_contract都含该pattern，但模型正文均未遵守，服务端以string_pattern_mismatch拒绝；不能据此泛化提供者全部regex支持。初稿无来源否定纸张结构，修复稿仅复述动作，实际结果和未知仍只在覆盖字段。e68新揭示／e69搜索／e74账本保留，e75不完整、e76正式回退、e81 attempted，25:7的observe待完成。6次调用、27847 input／1924 output，138.272秒；两稿各1delta及重连快照，均interrupt，无成功end。见[最后浏览器报告](../data/prepared/batch-49/real-10/browser-observer/report.md)。

固定措辞pattern实验没有改善真实结果，**最终代码已撤回**，避免额外限制合法同义表达。完整结果正文字段、明确未知需求、简短结果指令和原逐项语义校验保留；不从coverage复制成正式成功答复。撤回后将09／10原稿、错对象及先否定后未知冻结，最终定向 **107通过，1.32秒**，见[evidence-final-12](../data/prepared/batch-49/evidence-final-12.txt)。不继续相同代码概率重试；本题未通过的结论保留。

## 最终通过范围与交付

| 验收项 | 结果与边界 |
| --- | --- |
| 最终已选原文来源、混合读便签 | 冻结48首败及反例通过；real-03/read正式正文答全，同次成功attempt2增量／响应中重连／最终history一致 |
| 指定旧重叠姓名猎人、徒手对象 | 原话正确路由到唯一猎人；短名编辑无触发。真实子回合执行，不因未持有便签或“手”误拒；歧义／引用／否定／未持有工具反例通过 |
| 本次新揭示与结果账本 | 新hidden→revealed事件、实际执行者／目标／源动作持久保留，旁白失败不抹效果；旧揭示和错请求不结清 |
| 自然委派KP正文、完整请求与成功流 | **未通过**。真实结果已发生，但正式正文未答全原检查需求；search有本次证据，observe待完成，任务attempted。失败流不能借用read的成功流算通过 |
| 保卡文字修复 | 假模型耗尽／审计／HO权限边界通过；真实同一人物第二次只修文字成功，卡、原骰、seed、已定姓名及其他成员完全保留 |
| 人数前置、减少人数、服务端恢复、继续旧房间 | 后端及真实浏览器假模型流程通过，超员0调用阻断，清缓存与新浏览器续办不重复生成，减少人数不重抽保留成员，旧房间原地解锁保持玩家投影 |

本批真实本地模型合计 **58次**（自然56、人物文字2），实际usage **285918 input／19003 output tokens**，模型记录延迟合计1050691ms；自然各失败／复验墙钟合计1303.593秒，人物两次38.311秒。每次独立耗时见[调用与保护审计](../data/prepared/batch-49/real-metrics.md)，不能合成一次正常回合耗时或宣称提速。外部API调用0，向导Fake文字4次另计。

原稿、修复稿、正式事件与流式／重连全部分别留存。原库／WAL及受保护12路径哈希、旧6张卡／68骰／33事件／6档案／1准备及44实体／6席位逐行核对保持；新房间复用3张完整卡及档案／批准准备，初始背面hidden。最后停服后再次核对保护；本批Chrome、前端观察服务与隔离后端已退出。前端build／lint、变更Python Ruff与正常仓库行尾设置下的diff检查通过。未重跑全量669卡或整模组，窗口16384、正常回合步骤及一次修复预算不变；未commit/push。
