# 第二十一批：普通实物交互与车卡扩展

日期：2026-09-15。执行分支 `main`；开始时 HEAD 为 `fa0e9178ebc44d32a721c1292af5f5ce59dd30ac`，工作区无已有修改。单执行者，未创建 worktree，未 commit/push。

## 交付状态

| 项目 | 本批结果 |
| --- | --- |
| P0 普通实物 | 报纸、门上便签补首次取得、交接、放下、原地拾回；新动作覆盖旧调查焦点；实例与存档保持一致。真实模型短复验见下文 |
| P0 理智、扩展检定、基础战斗 | 保留；相关回归通过，另完成来源明确的 B 终幕短复验 |
| P1 运行时卡 | 新卡专业进入快照与检定；原有装备与实际持有分开，武器按实际实例投影到既有战斗服务 |
| P1 车卡 | 规则 1.1.0：103 个技能 ID、31 个职业、职业公式/属性及分组选择、信用资产、背景、装备；前端完成两个职业的完整创建流程 |
| 高龄 | **90 岁及以上未完成**。本地概述写15–90，详细调整表只到80–89；未发现90+完整条款，继续明确支持15–89，不外推官方数值 |
| 长测 | 沿用第二十批最终房间77分58.10秒及两次正常恢复各8/8的结论。本批是定向短复验，不累计为新长测 |

## 1. 普通物品：数据与行动同时修复

已先读第二十批报告、coverage、主持调试记录，并只读核对本地原稿。原批准包 SHA256 仍为 `b47bf0fb5127e4bc46fdb1c5a97fd9b41fe358eea2595351b5e19d95411e6c87`。

新包在 `data/prepared/changan/batch-21/package-approved.json`，SHA256 为 `93dee39824e41a0d9544fc5e869845c0ffcaeb43dd54603aec1eae840372ddd4`；`package-diff.json` 记录完整前后差异。NPC v1 数值、原检定、揭示条件和旧包未改。

新包静态审计通过来源完整性：243段、44实体、7可玩场景，必需NPC操作缺口为空。历史审计仍有2项blocked、4项host_ruling，`runtime_complete=false`字段保留；本批没有把原未知武器/可选分支改称已完整支持。

- 原稿 Word 页2–4：报纸是实物；便签可携带，正面改为物品，背面仍是独立线索。各补四种库存操作，携带不等于发现背面或解读日期。
- 门旁示意图是固定设施；散落行李是障碍集合，未虚构其中可取物。手机/手电沿用起始遗失与找回规则，钥匙仍遵守保管人的异议。
- 新的明确实物动作重新绑定当前对象与动作，清除无关旧检定/揭示/工具/转场焦点。无障碍且唯一的库存操作直接走现有执行器；有障碍的交互继续由 KP 裁定。
- 首次取得与拾回分开。同一物品已有持有、落地或消耗记录时不会重新生成；交接、放下和拾回保持实例 ID。重复拿取明确反馈当前持有状态。
- “我对队友说：请交给我”保留为队友请求，需队友实际行动后才转移。多件同名物品可附实例编号，拾回也按指定实例执行。
- 阅读只呈现当前可接触实体的已公开描述。修复报纸类型为物品时只说“开始朗读”而未呈现正文的缺口；不提供隐藏日期或隐藏背面。

### 正式模组真实模型短复验

`short-c2` 由第二十批 `long-c39` 只读备份生成，通过正常接口加载中途存档 `47b82287-a691-4b7d-a923-57a9422ff320`。复制脚本仅把本批声明的两个实体差异应用到副本；来源、三个数据库哈希及前后字段在 `fixture-provenance.json`，没有修改原库、原骰或原房间。

| 验证 | 实际事件 |
| --- | --- |
| 调查失败后拿报纸 | 原#462发现报纸；原#497日期调查92/20失败。#1773首次取得，未重做调查 |
| 给队友、换场景 | #1804交沈岚；#1834转到4号车厢；#1981呈现报纸公开正文 |
| 返还、放下、拾回 | #2078沈岚实际返还；#2108放下；正常保存重启/读档后#2143拾回 |
| 重复请求 | #2163提示已经持有；实例仍是 `714ba467-8615-4d67-9f73-6f90de16cd35` |
| 不同实物/改写 | 回到6号车厢，输入“我把门上已发现的便签收进口袋”；#2285首次取得便签，未新揭示背面 |
| 事实回顾 | #2319正确回顾日期检定失败及当前两件持有物；该回合无新物品操作、检定或转场。多问中的“谁发现”未明确答全，不把这轮当作全问答命中 |

全段未产生新检定，最终日期实体仍隐藏。共269次模型调用含历史203次，本段新增66次（包括保留的失败/澄清候选）。最终保存 `08ac71e6-584f-40dc-9be0-6a3efe649918` 后正常重启、加载，8/8比较通过，报纸与便签仍由周衡持有。

失败候选保留：第一次转场在副本中因实体准备字段缺少默认值报错。修正复制脚本，另以 `normalize_batch21_fixture.py` 仅补副本默认字段，正常重试恢复；`fixture-normalization.json` 记录过程。返还过程中出现口头拖延、复杂请求澄清；只有#2078的执行回执计成功。后续转场仍有队友重复旧阅读台词、对便签的无依据推测；没有授予物品/线索，未扩展为通用台词优化。

### B 终幕与奖励：单独短复验

`short-c1` 同样来自 `long-c39` 的只读副本，沿用原角色和实际骰。它不是新开全流程，也不并入长测时长。

- 玩家从沈岚取得钥匙后，#1877真实完成减速并进入 B 终幕；此前未产生回执的减速台词不计成功。
- #1890：周衡 SAN 骰40，通过，实际损失2，50→48。
- #1908：沈岚 SAN 骰56，失败，实际损失8，50→42；INT15触发临时性疯狂，发作/持续时间按真实骰结算。
- #1969结算、#1971 `module.completed outcome=B`；两人神话各+3、SAN上限各96，末次结算没有再次扣除同一笔 SAN。
- 最终保存 `1b588ef7-dc63-4e1e-941c-313edf952cad`。共233次模型调用，其中203次来自历史房间，本段新增30次。

## 2. 规则与车卡范围

来源和支持边界详见 [SOURCES.md](../backend/app/rules/definitions/SOURCES.md)，已核对本地《规则书1907》《调查员手册1.20》，没有下载替代版本。规则书PDF27、30、37、47另做页面图像核对。

- 技能：规则书PDF47基础表与详情48–69，103个稳定ID；原29个ID/基础值不改，母语/闪避仍随属性，神话仍不可购点。语言10、艺术/手艺12、科学13、格斗8、射击7、驾驶2、生存4、学问1项目录专业独立分配与检定；未实现任意自定义专业及跨专业可选加值。
- 职业：完整规则书基础28项（PDF29–31），另加手册会计师、消防员、护士，共31项；不是全手册职业覆盖。各条目保留来源、信用范围、EDU×4或EDU×2加指定/所选属性×2、分组与专业要求。使用年龄调整后的属性；切换职业重算并校验，分组不可重复计数。
- 信用：PDF36–37，1920年代/现代六档现金、资产、日常消费额度，美元表值；99档资产为下限，其他档资产明细不可超总资产。日常额度不会每天自动增加资金，信用点数规则保留。
- 背景：外貌、信念、重要之人、地点、宝贵之物、特质及关键联系；随草稿、确认、导入导出保存，沿用完整角色资料可见权限。
- 装备：9项普通目录和4项已核对武器，数量、备注及自定义物品。自定义物品无自动加成；枪械默认空弹，弹药沿用已有主持配置与战斗流程。自动射击、霰弹分档、爆炸范围、链锯/绞索等特例未实现。
- 原有装备在角色快照，实际持有在现有库存。开团只初始化一次；有起始随身物结算的模组先标记待结算，不自动发放卡上装备，不能绕过《常暗之厢》的起始遗失规则。
- 旧数据：原1.0.0 YAML原样归档，按ID和版本读取。旧草稿继续按原版编辑、旧卡可导入/导出；已确认卡和房间快照不迁移重算，原始骰保留。新增可选字段有空值/1920年代等默认。

完整目录留在快照和 `inspect_character`；8192上下文内只投影原常用技能、职业/已选/已投入技能及本次点名技能，避免扩展目录挤掉事实和摘要。两条严格记忆预算回归在调整后通过，未增加模型上下文限制。

## 3. 验证证据

所有真实模型流程使用本地 `qwen3:8b`、8192上下文；测试夹具、真实模型短复验、历史长测分别计数。

| 类别 | 结果与证据 |
| --- | --- |
| 第一轮全量 | `all-c1.xml`：1368通过、4失败、1跳过；跳过原因为Windows无符号链接权限 |
| 定向收敛 | `final.xml`：744通过，覆盖规则、车卡API、房间、战斗、批19/20/21、事实回顾与记忆边界 |
| 最后物品修复复测 | `final-items.xml`：530通过，覆盖批20/21及行动裁定；与上行有重叠，不相加 |
| 前端 | lint/build通过；浏览器无异常；桌面及390宽度检查通过 |
| 浏览器完整创建 | `browser-c2/result.json`：随机医生、购点音乐家，各完成创建、保存、重载、确认、导出/导入；进程重启后数据与原始骰保持一致 |
| 新卡房间 | `new-card-c2`：导入浏览器音乐家卡，绑定仓库原创《停摆的钟楼》；物理学41、实际小刀及家书初始化；#64真实物理学82/41失败正常结算 |
| 装备使用 | `new-card-c2/equipment-smoke.json`：原小刀实例通过既有战斗API，42/25未命中；原创训练夹具的回应与防御正常结算并结束冲突。夹具不属于正式模组NPC v1 |

新卡继续通过自然语言#126放下小刀（实际武器栏移除）、#153拾回（同一实例恢复）、#185回顾物理学失败与当前持有物（无新操作）。最终保存 `1f5dd22d-f571-4557-aaa5-49a456874e9a`，正常重启/读档后8/8比较通过，包含专业检定、背景、原装备/实际实例及战斗状态。该房间共13次真实模型调用；战斗配置与人工防御选择属于单列的普通API夹具操作，不计为模型自行完成。

`new-card-c1`是修正初始化格式前的未验收候选，不用于结论。装备脚本首轮因未先登记调查员战斗参与者而收到404，未产生攻击骰；补齐正常setup流程后才进行上述真实攻击。最终证据索引见 [batch-21-coverage.json](batch-21-coverage.json)。

第一轮四处失败已分别处理：两个由扩大技能目录导致的提示预算问题、运行时卡新增默认字段的旧断言，以及批9仍期待旧回顾前缀的过时夹具。`git show fa0e9178:backend/app/agents/generation_contracts.py` 已证明基线采用原文事实呈现；更新夹具后继续检查历史范围、读档分支和回顾不产生操作，未改变生产回顾行为。

旧1.0.0归档与基线规则文件的Git blob均为 `b3aaf70ecd02e49be40bb47a79cb8d4625871c7c`。最后 Ruff、`git diff --check`通过。两间本批房间各做事实回顾和恢复复验，没有发现本批引入的持续记忆/调度故障，因此未追加长测。

## 4. 第二十批摘要计数核对

只读核对 `long-c39/summary-range-index.json`、原 `HOST_DEBUG-events.json` 和原数据库：索引47项，对应47条 `agent.summary_rebuilt`；另有#1196一条 `agent.summary_stale`。coverage中的48混入了这条失效通知，已更正为47并添加审核说明。历史77分58.10秒、主线/记忆范围及两次8/8恢复结论保留。

## 5. 交付与限制

运行入口见 [精简运行清单](batch-21-checklist.md)。本地原文、数据库、冻结包、模型调用和浏览器产物均留在 `data/prepared/changan/batch-21/`，不把私有原稿或带令牌的会话文件提交Git。旧包、历史库受哈希核对保护。

明确未完成项是90岁及以上规则；职业/专业和装备是上述有限目录，武器特例见已有基础战斗文档。普通叙述仍有重复与即兴偏差，执行回执与事实回顾保持独立。本批未重跑历史长测，也未开展模型/API切换、一键启动、公网联机或P2。
