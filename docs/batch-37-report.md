# 第37批：自然交互阻断修复与真实游玩

本报告随验证更新；最终验收与统计尚在执行，不将中间调试结果记作通关。

## 实际基线与材料

- 本地 `main` 的 HEAD 为 `c75aea2a855c5dc1836fa2ed3ea850b3ee7b93b1`，包含第36批修改与报告。开始时工作区干净；执行 `git pull --ff-only` 返回 Already up to date。远端 `origin/main` 为 `7337ef285023902c2455a8a2478bd1fa4c9544de`。本次依据本地代码和私有审计定位，没有以第35批源码代替第36批现场。
- 始终使用当前分支、单执行者；未创建分支/worktree、启动子 Agent、reset/stash、commit 或 push。
- 原始现场为 `data/prepared/changan/batch-36/run-20260917T235205Z`。本批目录为 `data/prepared/changan/batch-37`，历史资料没有被覆盖。保护清单见 `protected-files-initial.json`。
- `baseline-evidence.json` 按玩家输入、周期、实际模型输入、生成输出、修正、最终计划和事件汇集证据。第36批成功调用保存的是适配器校验后的 `generated_output`，没有单独保存原始 JSON 与实际 JSON grammar；这些环节只能结合当时代码复建，不能冒充原始审计。本批新增 `raw_output` 与 `output_contract`。

## 阻断的证据链与修改

| 第36批输入/公开事件 | 首个问题环节 | 本批处理 |
| --- | --- | --- |
| 26 → 44/50 | “车头”未被方向解析识别；模型选择描述为后方的7号出口，旁白仍复述前进 | 从批准连接及带明确前后含义的端点建立通道方向，沿无分叉连接传播；车头/前一节按稳定方向，退回按上一场景，分叉或冲突停止推断，不比较车厢编号 |
| 127 → 135 | 正确移动计划夹带无关危险检查；检查被拒后，修正把整步改成澄清 | 保留唯一明确移动及目标，冗余场景节点不作为实体揭示 |
| 209/233 → 217/241 | 检查与报纸目标原已正确，揭示因上下文缺失被拒；修正在补上下文之前破坏原尝试 | 纯 `context_missing` 先进入已有上下文补充，不提前重写行动 |
| 438/462 → 446/470 | 462 的3号移动合法，额外 `node_current` 揭示非法；修正声称出口不合法并添加澄清 | 清除路由节点的冗余揭示，恢复正确 move 已有的目标及非澄清状态；条件仍由原裁决检查 |
| 58 → 80；后续杂物查看 | 普通观察与无状态写入混同，结果常只复述尝试；场景即兴记忆未完整提供给 KP | 场景范围内继续利用即兴记忆，观察契约直接要求可感知内容，资源与关键线索仍受原裁决约束 |
| 319/348 → 340/369 | NPC 最终提示主要剩伤者介绍；已批准的可知证词未进入答话依据，普通问候也被证据逐字要求挡住 | 读取同场景、自动披露且通过条件检查的 NPC 证词；问候依据当前伤势和清醒状态；模型选证据 ID，服务端补原文 |
| 378 → 399/401 | “我可以帮你做些什么”被切成玩家动作；称呼残片阻止恢复完整请求；历史文本成为当前回答 | 修正对象与完整问句归属，当前任务优先；“我问……刚才发生什么”保留为新对话，不强制回放历史记录 |
| 112 → 123/124 | 同场景任意隐藏检查要求都可能触发未绑定搜索修复，连无关门口查看也被挡住 | 只要求与当前目标或原话匹配的隐藏检查；普通观察无需认领无关线索 |
| 288 → 302 | “我跟上，看看有没有能帮上忙的”被当作集体转场 | 共同场景中的跟随作为小动作/台词处理，不产生空移动子周期 |
| 护士多轮口头承诺 | 纯承诺可被当作接受任务；旧附带台词又会否决新的实质行动；观察结算会清掉整个急救请求 | 接受时要求角色自己发布具体尝试；重复附带台词不抹掉新动作；按实际尝试部分回流任务，技术失败保留请求 |

NPC 的可披露依据仍由批准实体、当前位置、披露条件和实际对象共同限定。未知关键真相不会因寒暄资格而获得补编权限。单个回答失败时保留其他已校验答案；回退标记如实记录。

## 验证与实测记录

所有真实短场景使用本机 Ollama `qwen3:8b`、8192 上下文，KP 与队友均实际调用。自动化回归使用测试替身并隔绝网络；它们与自然交互质量分开评价。

已恢复第36批4号车厢现场：`debug-01/move43` 的 seq 490 输入“我现在走进3号车厢”，seq 508 实际转到3号，seq 514 旁白正确，没有澄清。后续短场景及完整调试局证据如下。

| 运行 | 性质 | 已确认结果 |
| --- | --- | --- |
| debug-01/move43 | 恢复现场的定向调试 | 4→3 成功进入原导航裁决并实际转场 |
| debug-02 至 debug-06 | NPC、护士与观察调试 | 保留失败、旧话重播及未执行治疗的证据；均不作为最终验收局 |
| short-01/observation | 独立普通观察短场景 | 无动作澄清，但部分回答仍空泛、存在回退；不夸大自然质量 |
| short-02/npc-continuation | 延续调试状态 | 护士实际发布止血并掷出急救19；发现自然词“止血”未接入伤势结算，之后修复路由，未重掷这一原骰 |
| short-04/navigation | 独立方向/队友短场景 | seq 508 到3号，539 退回4号，545 旁白一致；588 陈拓跟随只作同场景回应，无空子周期。护士主动急救于564结算 |
| short-05/followthrough | 任务续行调试 | 护士承诺缺少尝试被拒时保留请求；603 发布本人行动，619 医疗成功；同时发现“我能帮你做些什么”被误判委托，随后修复 |
| short-06/result-followup | 独立结果追问 | seq 643 护士依据真实治疗结果回答伤情稳定并提出后续方向，没有再次治疗或把任务归给玩家 |
| run-20260918T092616Z | 新房间调试局 | 发现省略号后的行动被错误请求覆盖、隐藏事件主目标挤掉有效检查；已保存并保留所有失败公开事件，不作为最终验收 |
| short-07/inspection | 恢复新阻断现场 | seq 166 侦查请求，170 原骰9，174 公开遗留报纸；没有提前揭示车厢消失。179 旁白使用带实际结果的回退，计入质量缺陷 |
| run-20260918T101500Z | 第二个新房间调试局 | 正常建卡后继续完整路线定位；泛泛跟随变体、叙述式问话对象切换及医疗失败状态回流继续修补，保留原骰与失败 |
| short-08/mixed | 对象切换调试 | NPC问候和受伤原因已回答，护士“处理伤口”仍只说不做；该措辞后接入既有急救识别 |
| short-09/mixed | 混合请求独立验证 | seq 256 NPC分别回应听觉与遇袭；259护士本人急救，270医疗结算，原骰65/技能60失败，275公开真实失败，不将治疗归给玩家 |
| short10/passage-order | 独立通道检定验证 | seq 703实际请求潜行，707原骰57、709失败结算；没有澄清或免骰通过。旁白两次回退，不能计作表达合格 |
| short11/settled-route | 独立已结算通行窗口验证 | seq 1048进入先头车厢，无澄清、无追加检定；1054为正确位置的回退旁白，仍计质量缺陷 |

新现场的 seq 82 原始输出同时含本人动作与覆盖同片段的虚假队友请求；`speaker_action` 漏掉省略号前缀，导致后处理删除正确行动。按真实分句恢复后，seq 112 又暴露出“主目标=时间压力线索、检查目标=报纸”的冲突：旧恢复删除了合法检查，留下无关揭示。修复将已选择的合法调查目标保持到裁决，移除被替换主目标的揭示；免检定候选同时保留批准的私有披露条件，避免上下文压缩把可选事件表述成已经满足条件。

第二个调试局继续到3号：seq 343 明确走进3号，364真实转场，370旁白正确；379寻找黑包，400侦查原骰11通过，404公开黑包，409解释具体发现。这不是靠直接改状态推进。该目录名称是运行标签，实际时间以事件 `occurred_at` 与审计时间为准。

进一步修复了三个同链路遗漏：同场景泛泛跟随中“先看看车头有没有什么异常”等变体；“我蹲在乘务员旁边问”的显式对象边界；医疗子周期失败从 `combat.resolved` 回流为 `blocked`，不再因周期完成就记录治疗完成。NPC最终生成中剔除分给队友的当前问题，原始整句仍留在私有审计。

调试局后半段进一步暴露了执行顺序问题：seq 615 已有合法潜行计划，但其自动通道目标直到状态执行阶段才公开，检定阶段先跳过不可见目标，后续方法又因无实际检定被拒。现仅将已获批准、本地、自动公开的检定目标提前公开，并沿用同一工具回执重新校验；必需检定不省略。必要 HTTP 回归验证公开事件先于检定请求，且尚无骰点和成功效果。

该局 seq 712 的声音诱导已真正产生通行机会，726 的后续移动却再次选择潜行方法。现按实际路线要求和已结算状态停止重做同一障碍，独立检查仍不跳过。修复后 seq 823 实际进入先头车厢、829旁白一致。队友“我注意到你试图……”属于对他人尝试的评论，现作为发言处理，不再把其中动词转为本人的新行动。

第二个调试局最后用实际钥匙开门（857）、解锁面板（890），依据公开说明向上推动减速杆（933），触发结局B并逐人结算理智损失（1022），实际 `module.completed` 为1024。其21次玩家输入、8次队友尝试、1次澄清、136次模型调用、19次调用校验/生成错误、4次旁白回退均包含修复前结果，不能作为冻结版本验收指标。模型总耗时1,036.772秒；应用残差、等待与各周期明细见该局 `timing-summary.json`。

阶段回归：较早受影响回归142项通过、省略号与跟随修复后99项通过。较广检查 `regression27.log` 为155通过、3失败；这3项旧导航测试仍预期条件不满足即进入主机审核，使用本地HEAD全部相关应用模块在内存中复跑得到相同失败（`baseline-navigation.log`、`check-baseline-navigation.py`），没有切换工作区或分支。后续发现的非准备模组NPC回归属于本批问题，已限定自动披露逻辑只用于准备模组，`regression37.log` 两项定向验证通过。中间测试设置错误和失败日志完整保留，不计作通过。最终受影响回归结果另列。

保护核对 `protected-files-verification.json`：65,575个原有文件，改变0、缺失0；核对耗时684.06秒。该清单与核对结果属于私有交付材料，不是公开团录。

最终代码版本、未提交差异、关键文件哈希分别保存在各运行目录的 `code-version.json` 与 `code.diff`。最终验收局会另列冻结版本，任何中途改码的局均标作调试局。

## 后续局部观察与任务续行

候选 `final-20260918T112200Z` 的“我跟上你，先检查一下车厢有没有异常”仍启动空子周期，已纳入同场景跟随判定。`short12/follow` 中 seq 111 自然跟随回应，无空行动或澄清。

候选 `acceptance-final` 的 seq 117 “我过去看看前面的车门有没有异常”在归属恢复后已是观察，但残留转场和目的地揭示，使整步被权限检查拒绝。后处理现将动作、路由及目的地揭示一起校正，不改变队友无权替全队选路的边界。`short13/door-observation` 又发现技术失败的未执行请求被自己的旧输出和冷却挡住；仅对仍待办且上次结果为技术失败的明确请求允许再次自行决定尝试，真实检定失败不享有此例外。`short14/door-retry` 的 seq 226 给出门关闭、有缝隙的当前观察；253 后续回答利用此信息，但仍有“过去看看能不能打开”的未执行承诺，不能计为已开门。

候选 `acceptance-20260918-final` 的 seq 90 “我过去看看车门情况”则被模型分类为 move，当前普通物件没有模组实体，较早的同场景恢复条件未命中；其 seq 116 技术回退，实际原骰34和报纸发现均保留。现在仅对无明确出口、以靠近并观察为实际目的的动作恢复本场景观察，穿过、进入、退回和已明确命名出口仍沿导航校验。两种原句加入相同 HTTP 行为回归，验证最终公开观察且无场景转移。该候选也标为调试局，没有以停止当作通关。

最后一次较完整受影响检查 `final-affected-regression.log`：94通过、21警告。此前 `final-regression.log` 为154通过；这两组有重叠，不能相加宣称覆盖数量。最后局部观察变体的定向检查单独记录。全部修改文件 Ruff 已通过。

保护文件的第二次完整复核 `protected-files-final-verification.json`：65,575文件，改变0、缺失0，耗时757.23秒。后续应用修复与新局仍只写源代码、报告及本批目录。

普通观察的恢复随后分两层验证：`short16/door-only` 已解除队友移动权限拒绝，但错误绑定旧报纸；`short17/door-target` 目标恢复本场景，生成仍被旧旁白污染。只修提示中的当前观察范围，按对象相关性保留即兴记忆、剔除无关旧问句和资料；权威上下文与原始记忆不删除。`short19/door-perception` 的 seq 254 玩家、283 陈拓均给出便签外观，无转场或澄清、无旁白回退，内容仍较单薄，不等同关键发现。`short18` 的队友问答还出现了未经实际观察确认的“别碰车门”说法，作为质量缺陷保留，未作为资源或模组真相执行。

`short15` 输入同时要求队友看门并说“咱们再往前走”，得到当前查看还是立即过去的具体澄清，保留该记录，没有将它称为普通对象不明确。它不用于证明单一明确动作通过。

最终搜索/动作相关回归 `final-observation-regression2.log` 为80通过、17警告；最后提示过滤修复另跑 `final-prompt-regression2.log`，4通过。中途错误测试路径及漏导入导致的失败日志未删，不计入通过。Ruff最终已通过。

`acceptance-20260918-v2` 也归为调试局：seq 58 的本人“我翻翻……”被模型作为队友问题，因口语叠词未进入既有搜索动词判断，归属恢复未拦下。仅扩展现有搜索/明确尝试词形，不另建解析框架。保存并原局续行后，seq 131 实际请求侦查，135 原骰49、137失败，145反馈未发现线索；陈拓自主提出另一角色的检查（149），原骰93于172失败。没有改骰、补成功或把这两个失败当作技术错误。护士147把玩家尝试说成自己的经历，是仍存在的角色归属表达缺陷，应与已经修好的实际工具归属区分。对应 HTTP 回归 `final-search-owner-regression.log` 两项通过，Ruff通过。

继续原局时，seq 185 的“朝车头、前一节”错误到达6号（202），208仍说向前；这是早期方向修复不完整的反证。实际批准出口只含场景正文，5号两个目标的正文都没有前后标签，原先仅排除“后方”候选的方法没有稳定轴线。

方向修复补在既有事实装载与策略中：从批准场景端点的明确前/后描述及实际连接建立有向依据，沿度数为2的无分叉通道传递；分叉停止，冲突不推断。局部出口获得服务端 `direction`，同一值进入模型上下文、计划恢复和权限裁决。“退回”仍读取真实上一节点。没有使用车厢编号、候选排列或本局固定路线。旧错误移动保留在公开事件，保存后从实际6号续行，seq 236到5号；并未修改世界位置掩盖失败。

方向修复后，seq 250再次朝车头，269真实进入4号，不再回到6号；275旁白却仍复播旧便签和“空无一人”，与已公开伤者矛盾。到达新场景的生成提示现剔除旧到达段落与旧对白，只保留本次转场、当前场景及状态依据；权威记忆不删除。回归 `final-direction-regression.log` 27通过，`final-arrival-regression.log` 2通过，相关文件Ruff通过。

同一调试局的混合救助在 seq 286 提交。312乘务员回答听得见、伤痛和怪物袭击经过，并依据已批准证词说明声音诱导；315周岚以本人发布具体处理伤口尝试，331实际急救成功。治疗没有归给玩家，也不是仅凭“我来处理”的台词宣称完成。

调试局 seq 356乘务员给出驾驶室钥匙及黑包掉落位置，358护士根据真实急救结算说明止血、状态稳定并提醒后续观察，没有再次治疗。随后正常保存恢复（366–368），`restore-verification.json` 的 `equal_checks`、`equal_receipts`、`replay_unchanged` 均为true，重交已结算骰获得幂等回执。恢复后369明确4→3，388真实进入3号，394旁白与新场景一致，没有旧便签重播。

冻结版本完整本批回归 `frozen-batch37-regression.log`：43通过、18警告，238.62秒。既有Pydantic序列化及测试中空主键警告未当作通过之外的行为证据。此前受影响搜索/裁决80项、方向27项、合作94项有重复覆盖，分别列出，不相加。

候选 `acceptance-frozen-20260918` 后因NPC局部修正错误归为调试局；其初始版本 HEAD 为 `c75aea2a855c5dc1836fa2ed3ea850b3ee7b93b1`，启动时未提交diff的SHA-256为 `1903a25c5f6404913fa814ec0df0e6b0bdc448676e9e4564672b9cfe976295e9`，150个关键文件哈希在 `code-version.json`。后续报告修改不代表应用版本变化，应用、测试和驱动文件将在结束时逐个复核。

`acceptance-frozen-20260918` 候选的移动、急救实际链路已执行：45到5号、115到4号，121旁白正确；128混合救助，162乘务员回答听觉与袭击，165护士本人包扎，180急救成功。但183追问驾驶室和钥匙后，206的答话先称钥匙在自己这里、又对“在身上吗”用未知模板，208护士沿用了错误说法；因此取消该候选验收资格。

私有调用显示初次第一项“我保管过驾驶室门钥匙，但逃跑时黑包掉在3号车厢前门附近”本来有效，第二项却重播旧问候。修正提示没有指出出错问题索引，修正结果反而把第一项写成现在持有。局部回退再按倒序选调用，优先拿了修正的答案，跳过原先有效项。现按尝试顺序保留已验证答案、明确修正项索引与当前问题，并拒绝用“曾保管”的证词证明“现在在我这里”。这一修复不扩充NPC知识，也不制造钥匙位置。`final-npc-repair-regression.log` 两项通过，其中HTTP回归验证最终公开发言保留原有效项而非修正后的替代说法。

保护文件末次复核 `protected-files-delivery-verification.json`：65,575个原有文件，改变0、缺失0，752.31秒。

NPC后续短场景 `short20`、`short21` 暴露了另一层拒绝：历史叙述“逃跑时我的黑包……”被按当前持有物检查，导致正确过去经历也不能发布。现在仅在NPC口述物品校验中分离明确历史子句，当前使用或持有声明仍受原库存约束；实际行动权限没有放宽。`short22/npc-past-items` 的240保留本轮两次输出中各自有效的答案，说明曾保管钥匙、黑包掉落地点以及当前持有不确定。它仍用了第三人称，且两次生成均未整体通过，计作NPC局部回退，不记为自然表达完全合格。定向NPC回归8项通过。

`short22` 的242护士又沿用了旧“钥匙在他身上”。实际提示把5号车厢的“咱们别走散了”“先找人吧”保留为当前待回答问题，同时注入护士上次同样的错误答案。既有任务登记现排除无指向、无问句的普通群体表态，存档中这类残留也在原任务登记位置清理；真实询问、委托及技术失败请求不因此丢弃。过时的本人意见不再以行动结果字段重复注入新轮提示。统计增加周期级叙事回退和NPC局部回退，保留原公开回退计数，避免漏计。

## 最终结论

候选 `acceptance-37-final-run` 保存于4号并改记调试：181的“处理一下腿上的伤口”漏过治疗动词识别，后半句“蹲在他旁边问”又沿用护士对象；202只有口头承诺，没有NPC答话和治疗。现在既有医疗词形接受伤口部位修饰；前句明确请求治疗、当前只有一个可交互NPC时，后句“蹲在他/她旁边问”绑定该伤者，原句不改写、多个NPC不作这一推断。`short24/mixed-pronoun` 原句真实复测：249分别答听觉与受伤，251护士本人具体急救，267实际成功；SAN原骰47及损失2保留。8项定向行为回归、5项归属边界回归通过，中间夹具字符串错误的失败日志保留。

待关键短场景、自然保存恢复、最终新局实际结局及保护文件复核完成后填写。功能测试通过、生成 JSON 成功和说话次数增加都不等于自然通关或队友有有效贡献。

最终代码回归 `final-batch37-regression.log`：48通过、19警告，256.61秒；修改文件 Ruff（`final-ruff.log`）与 `git diff --check`（`final-diff-check.log`）退出码均为0。最后任务残留复测 `short23/current-requests` 未再次调起护士回答旧任务；241仍有一项NPC未知回退，但保留黑包位置，计作表达质量未完全解决。
